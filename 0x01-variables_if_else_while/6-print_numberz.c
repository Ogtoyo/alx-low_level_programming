#include <stdio.h>

/**
 * main - printing the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		putchar(num);
	}
	putchar(10);

	return (0);
}
