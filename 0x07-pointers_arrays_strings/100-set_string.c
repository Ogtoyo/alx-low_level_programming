#include "main.h"
#include <stdio.h>
#include <unistd.h>

/**
 *set_string - a function that prints sum of two diagonals of matrix
 *
 * @s: pointer to a pointer to a character
 * @to: pointer to character
 *
 */

void set_string(char **s, char *to)
{
	*s = to;
}
